
### 빌드
| 상황            | 권장 명령                     |
| ------------- | ------------------------- |
| 개발 중 로컬 실행    | `./gradlew bootRun`       |
| 정상 빌드 & 배포    | `./gradlew build`         |
| 테스트 제외 빌드     | `./gradlew build -x test` |
| 실행 JAR만 급히 필요 | `./gradlew bootJar` (비권장) |
